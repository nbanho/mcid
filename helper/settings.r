intervention_col <- c(cbPalette[1], "black", cbPalette[2])

classes <- c("Study (A)", "Study (B)", "Study", "Control")
classes_names <- c("Intervention (A)", "Intervention (B)", "Intervention", "Control")
names(classes_names) <- classes